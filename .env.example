# Required: Password for admin panel
ADMIN_PASSWORD=changeme

# Optional: HTTP server port (default: 8080)
SERVER_PORT=8080

# Optional: Directory for SQLite DB and tips.md (default: ./data)
DATA_DIR=./data
